# Deploy: em push para main roda CI (uma vez) e depois deploy.
# CI em PR é feito pelo workflow ci.yml.
name: 🚀 Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  ci:
    name: Run CI
    uses: ./.github/workflows/run-ci.yml

  # Deploy via SSH (desabilitado - front está na Vercel, não há VPS para o front)
  deploy:
    name: 🚀 Deploy to Production
    needs: ci
    runs-on: ubuntu-latest
    if: false

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # A chave em SSH_PRIVATE_KEY deve ser SEM passphrase (webfactory/ssh-agent não suporta)
      - name: 🔐 Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🔍 Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: 🚀 Deploy to server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          APP_DIR: ${{ secrets.APP_DIR }}
          NODE_VERSION: '20'
        run: |
          echo "🚀 Iniciando deploy do front-end..."

          ssh $SERVER_USER@$SERVER_HOST bash << 'EOF'
            set -e
            cd $APP_DIR || exit 1

            echo "📥 Fazendo pull do código..."
            git fetch origin
            git checkout main
            git pull origin main

            echo "📦 Instalando dependências..."
            yarn install --frozen-lockfile

            echo "🏗️ Construindo aplicação..."
            export NEXT_PUBLIC_API_URL="${{ secrets.NEXT_PUBLIC_API_URL }}"
            yarn build

            echo "🔄 Reiniciando serviço (PM2/Systemd)..."
            # Ajuste o comando abaixo conforme seu gerenciador de processos
            # Para PM2:
            # pm2 restart gateway-web || pm2 start npm --name gateway-web -- start
            # Para Systemd:
            # sudo systemctl restart gateway-web || true

            echo "🧹 Limpando cache do Next.js..."
            rm -rf .next/cache || true

            echo "✅ Deploy concluído com sucesso!"
          EOF

      - name: ✅ Health Check
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          APP_URL: ${{ secrets.APP_URL }}
        run: |
          echo "🔍 Verificando saúde da aplicação..."
          sleep 10
          # Ajuste a URL conforme necessário
          curl -f ${APP_URL:-http://$SERVER_HOST} || exit 1
          echo "✅ Aplicação está respondendo!"

      - name: 📢 Notify deployment
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deploy concluído com sucesso!"
          else
            echo "❌ Deploy falhou!"
          fi
