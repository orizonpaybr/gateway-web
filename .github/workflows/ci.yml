# CI em PR: garante que a base está estável antes do merge.
# Em push para main, o CI roda dentro do deploy.yml (uma vez só).
name: 🔍 CI - Build & Test

on:
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  ci:
    name: Run CI
    uses: ./.github/workflows/run-ci.yml
